# 修改后推送到 GitHub

## 1. 只提交源码（推荐）

不要提交 `node_modules` 和构建产物 `dist`（GitHub Actions 会自己 `npm ci` 和 `npm run build`）。

在项目根目录 `plugin-vite` 下执行：

```bash
git add src/main.ts
git add vite.config.ts
# 如有其它想提交的源码或配置，可继续 git add

git status
git commit -m "fix: 商品主图用 getCellAttachmentUrls、tooltip 左图右文、中轴线 50/100 修复、性能调优"
git push origin main
```

推送后，仓库的 **GitHub Actions** 会自动构建并部署到 GitHub Pages。

## 2. 若你希望把 dist 也提交上去

若你本地已经构建好并希望把 `dist` 一并提交：

```bash
git add src/main.ts
git add vite.config.ts
git add dist/
git status
git commit -m "fix: 同上并提交 dist"
git push origin main
```

## 3. 不要提交的

- `node_modules/`（应在 `.gitignore` 里）
- 本地临时文件、个人清单等（如不需要可不必 `git add`）

## 4. 推送失败时

- 先执行：`git pull origin main --rebase`，再 `git push origin main`
- 若提示无权限：检查本机是否已配置 GitHub 账号（SSH 或 HTTPS token）
