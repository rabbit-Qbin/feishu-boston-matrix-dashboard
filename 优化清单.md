# 气泡图插件优化清单

**基线版本**：commit `c4e3236354f1693a36495c02d49b8f44ac959a23`（能加载 index-CkCJVTl2.js 的版本）  
**说明**：以下在回退到该版本后逐项修改。

---

## 模板复用说明（给其他人用）

- **直接用**：对方复制你的**多维表格模板**（表结构 + 字段名一致）并安装/打开同一插件时，**无需改任何参数**即可使用。
- **插件依赖**：
  - 数据表名：优先使用名为 **「选品结果」** 的表；若无则自动选「包含需求趋势得分、竞争强度得分、利润空间得分等字段」的表。
  - 字段名（需与下表一致，否则需改代码中的 `FIELD_NAMES`）：
    - 需求趋势得分、竞争强度得分、利润空间得分、综合得分、商品标题、ASIN、**初步产品分类**、**商品主图**
- **若对方表/字段名不同**：需要在本仓库 `src/main.ts` 顶部修改 `FIELD_NAMES` 及表名偏好（如「选品结果」），再重新构建部署。

---

## 数据获取说明（不是“调外部 API”）

插件运行在**飞书多维表格内部**（仪表盘 iframe），数据是**在飞书里直接拉取的**：

- 使用飞书官方 **多维表格插件 SDK**：`@lark-base-open/js-sdk`
- 通过 `bitable.base.getTableList()`、`table.getRecordListByPage()`、`record.getCellByField()`、`table.getCellAttachmentUrls()` 等方法，在**当前打开的 base/表**里读数据
- 这些方法走的是飞书客户端的**插件桥接**，不经过你自己写的后端，也不是“调第三方 API”
- 官方文档可参考：  
  - [飞书多维表格附件字段](https://feishu.feishu.cn/wiki/HcFVw4twtivAnykhOeec5mAMnth)  
  - [字节多维表格/插件开发](https://bytedance.larkoffice.com/docx/IahqdOrCto2xAKxPV7Oc8gyenfg)

---

## P0 - 必须修 / 必验证

| # | 需求 | 说明 |
|---|------|------|
| 1 | **分位数中轴基于当前绘图数据** | 中轴（需求/竞争中位数）用「筛选后的数据」计算；TopN 或筛选变化后中轴随之更新。 |
| 2 | **View 两阶段加载 + TopN 生效** | 选前 20/50/100 时只拉 2×N 条做排序再截断，不走「拉 500 条 + 批量并行处理」。 |
| 3 | **Top N 按配置字段排序** | 前 20/50/100 按「利润空间得分」或「综合得分」（与配置一致）排序后取前 N。 |
| 4 | **商品主图在 tooltip 显示** | 点击气泡时 tooltip 左侧显示商品主图（80×80），支持 URL/附件；无图则保持纯文字。 |

---

## P1 - 配置一致

| # | 需求 | 说明 |
|---|------|------|
| 5 | **配置保存后 View 能读到** | Create/Config 保存的「气泡大小/排序字段/筛选数量」在 View 中始终生效，不出现 filterLimit 变 all 或沿用旧配置。 |
| 6 | **View 初始化用最新配置** | `getConfig()` 正确读取 `filterLimit`、`sizeFieldName`、`sortFieldName`，无默认值覆盖。 |

---

## P2 - 性能与部署

| # | 需求 | 说明 |
|---|------|------|
| 7 | **两阶段批次/并发可调** | 按 500/1000/2000 条实测调批次大小与并发数，避免限流或超时。 |
| 8 | **部署后能加载到新 JS** | 解决 Pages/浏览器/飞书 iframe 缓存，确保部署后拿到最新代码（如 URL 带版本或时间戳）。 |

---

## P3 - 后续

| # | 需求 | 说明 |
|---|------|------|
| 9 | **顶部切片器** | 是否在插件内做顶部筛选或依赖多维表切片器，由你定方案后再实现。 |

---

## 建议执行顺序

1. 回退到 commit `c4e3236354f1693a36495c02d49b8f44ac959a23`
2. 按 P0 → P1 → P2 顺序逐项改、逐项验证
3. 每改一项：本地构建/部署后各测一遍
