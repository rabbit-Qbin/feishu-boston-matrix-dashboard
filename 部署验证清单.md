# 🚀 插件更新问题 - 一次性解决方案验证清单

## ✅ 已完成的修复

### 1. TypeScript 类型问题
- ✅ 安装 `@types/node` 依赖
- ✅ 所有 TS 错误已清除

### 2. Vite 配置优化
- ✅ 启用 `vite-plugin-add-version.ts` 插件
- ✅ 文件名添加时间戳：`[name]-[hash]-${Date.now()}.js`
- ✅ 构建时自动添加 `?v=时间戳` 参数

### 3. GitHub Actions 增强
- ✅ 详细日志输出
- ✅ 显示构建产物文件名
- ✅ 验证 HTML 中的资源引用
- ✅ Node.js 脚本二次保险

### 4. Git 提交推送
- ✅ 提交 SHA: `0c63679`
- ✅ 推送成功到 `main` 分支
- ✅ 触发 GitHub Actions 部署

---

## ⏳ 待验证（部署完成后）

### 步骤 1：检查 GitHub Actions 日志（2-3分钟后）

访问：https://github.com/rabbit-Qbin/feishu-boston-matrix-dashboard/actions

**关键验证点：**
1. ✅ 构建步骤成功
2. ✅ `Add cache busting` 步骤显示详细日志
3. ✅ 显示的文件名包含新的时间戳（不同于之前的 `index-CkCJVTl2.js`）
4. ✅ HTML 中的资源引用有 `?v=` 参数

**预期日志示例：**
```
📁 构建产物列表:
-rw-r--r-- 1 ... index-abc123-1740182400000.js
-rw-r--r-- 1 ... index-def456-1740182400000.css

🔍 验证：HTML 中的资源引用
<script type="module" crossorigin src="./assets/index-abc123-1740182400000.js?v=1740182400"></script>
```

---

### 步骤 2：验证部署产物（5分钟后，等待 CDN 刷新）

访问：https://rabbit-qbin.github.io/feishu-boston-matrix-dashboard/

**浏览器操作：**
1. 打开浏览器开发者工具 (F12)
2. Network 标签 → 勾选 "Disable cache"
3. 强制刷新 (Ctrl+Shift+R / Cmd+Shift+R)

**验证点：**
1. ✅ JS 文件名已改变（不再是 `index-CkCJVTl2.js`）
2. ✅ 文件 URL 包含 `?v=` 参数
3. ✅ Console 中看到新日志（例如 `🚀 [NEW] View loadViewData 两阶段加载策略启动`）
4. ✅ 不再有 `📋 开始解析记录数据（批量并行处理）` 旧日志

---

### 步骤 3：飞书端测试（最终验证）

**方法 A：强制刷新 URL（推荐）**
```
https://rabbit-qbin.github.io/feishu-boston-matrix-dashboard/?_refresh=1740182400
```

**方法 B：重新配置插件**
1. 飞书多维表 → 插件管理
2. 删除旧的 "波士顿矩阵气泡图" 插件
3. 重新添加，URL 使用上面的带参数版本

**预期结果：**
1. ✅ 点击气泡后，tooltip 显示商品主图（左侧 80x80px）
2. ✅ 选择"前20/50/100"快速加载（新的两阶段策略）
3. ✅ 中轴线正确（基于过滤后数据的中位数）
4. ✅ Console 显示新日志标识

---

## 🔧 核心修改点总结

| 文件 | 修改内容 | 作用 |
|------|---------|------|
| `vite.config.ts` | 启用插件 + 文件名时间戳 | 强制每次构建生成新文件名 |
| `deploy.yml` | 增强日志 + 验证步骤 | 透明化部署过程 |
| `package-lock.json` | 安装 @types/node | 修复 TS 类型错误 |

**机制说明：**
- **Vite 插件**：构建时处理（主力）
- **Actions 脚本**：部署时兜底（保险）
- **文件名时间戳**：彻底破坏 Vite hash 复用
- **?v= 参数**：破坏浏览器和 CDN 缓存

---

## 📞 如果还有问题

### 症状 1：文件名仍未改变
**原因**：GitHub Actions 构建失败  
**排查**：查看 Actions 日志中的错误信息

### 症状 2：文件名改了，但浏览器仍加载旧文件
**原因**：CDN 缓存未刷新  
**解决**：等待 10 分钟，或使用带 `?_refresh=` 的 URL

### 症状 3：飞书中仍是旧版本
**原因**：飞书 iframe 缓存  
**解决**：使用方法 B 重新配置插件

---

**当前状态**：✅ 代码已推送，等待部署完成（约 3 分钟）

**下一步**：3 分钟后检查 GitHub Actions 日志
